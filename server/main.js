import { Meteor } from 'meteor/meteor';
import { TodosCollection } from '../imports/api/TodosCollection.js';

//const insertTodo = todoItem => TodosCollection.insert({ item: todoItem });

Meteor.startup(() => {
  // code to run on server at startup
});
